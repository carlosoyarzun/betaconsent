# ai/

Configuración y prompts de IA en producción (distinto del tooling agentic de desarrollo en `.claude/` y `agent/`).

Gobierna: `SEC-CNS-###` (rutas críticas), `REQ-CNS-###` que involucren IA en runtime.

Estado: vacía (pre-build).
